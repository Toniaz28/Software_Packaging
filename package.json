{
  "name": "3mtt-packaging-node",
  "version": "1.0.0",
  "description": "Node.js packaging example for the 3MTT project",
  "private": true,
  "main": "node-app/app.js",
  "scripts": {
    "start": "node node-app/app.js",
    "build": "node -e \"console.log('Node packaging bundle ready')\"",
    "test": "jest --runInBand",
    "pack-artifact": "npm pack",
    "checksums": "python tools/checksums.py"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
